apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-php-app-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: my-php-app
            port:
              number: 80